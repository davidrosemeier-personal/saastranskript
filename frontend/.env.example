# Only needed in production, where frontend and backend are separate Railway services.
# Leave unset in local dev — Vite's dev server proxies /api and /auth to localhost:3000.
VITE_API_URL=https://your-backend-service.up.railway.app
