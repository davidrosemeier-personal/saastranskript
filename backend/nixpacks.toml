[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.build]
# --include=dev overrides NODE_ENV=production, which npm otherwise reads as a signal
# to skip devDependencies — but tsc (typescript) is a devDependency needed to build.
cmds = ["npm ci --include=dev", "npm run build"]

[start]
cmd = "npm run start"
